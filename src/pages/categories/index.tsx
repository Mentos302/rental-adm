import { NextPage } from "next";
import { Layout } from "shared/ui/layout";

export const CategoriesPage: NextPage = () => {
  return (
    <Layout>
      <section>
        <div className="heading">
          <h1>Категорії</h1>
        </div>
        <blockquote>В процесі...</blockquote>
      </section>
    </Layout>
  );
};
