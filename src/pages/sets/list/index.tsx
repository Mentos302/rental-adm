import { NextPage } from "next";
import { Layout } from "shared/ui/layout";

export const SetsListPage: NextPage = () => {
  return (
    <Layout>
      <section>
        <div className="heading">
          <h1>Набори</h1>
        </div>
        <blockquote>В процесі...</blockquote>
      </section>
    </Layout>
  );
};
